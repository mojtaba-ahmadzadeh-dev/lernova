export enum OrderStatus {
  Pending = "pending",
  Canceled = "canceled",
  Completed = "completed",
  Cancelled = "canceled",
  Paid = "paid",
}

export enum OrderItemStatus {
  Pending = "pending",
  Ordered = "ordered",
  Packed = "packed",
  InTransit = "in-transit",
  Canceled = "canceled",
  Delivered = "delivered",
  Success = "success",
  InProcess = "in-process",
}
